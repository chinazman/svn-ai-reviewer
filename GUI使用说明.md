# SVN 代码审核工具 - GUI 使用说明

## 快速开�?

### 1. 启动程序

双击 `svn-ai-reviewer.exe` �?`svn-ai-reviewer-gui.exe`，程序会�?
- 自动启动 Web 服务器（端口 8080�?
- 自动在默认浏览器中打开界面

如果浏览器没有自动打开，请手动访问：`http://localhost:8080`

### 2. 使用界面

#### 步骤 1：加载配�?
- 默认会自动加�?`config.yaml` 配置文件
- 如需使用其他配置文件，修改路径后点击"加载配置"按钮
- 加载成功后会显示 AI Provider �?Model 信息

#### 步骤 2：扫描变�?
- 输入 SVN 工作目录（默认为当前目录 `.`�?
- 点击"扫描变更"按钮
- 系统会自动检测所有变更文�?

#### 步骤 3：选择文件
- 文件列表会显示所有检测到的变�?
- 每个文件都有状态标识：
  - 🟢 **[新增]** - 新添加的文件
  - 🟡 **[修改]** - 已修改的文件
  - 🔴 **[删除]** - 已删除的文件
  - 🔵 **[未受控]** - 未加入版本控制的文件
- 默认全选所有文件，可以取消勾选不需要审核的文件
- 使用"全�?复选框快速选择/取消所有文�?

#### 步骤 4：开始审�?
- 点击"开始审�?按钮
- 执行日志区域会实时显示审核进�?
- 审核完成后会自动生成 HTML 报告
- 如果配置了自动打开，报告会在浏览器中自动打开

### 3. 查看结果

审核完成后：
- 日志区域会显示报告路�?
- HTML 报告包含详细的审核结�?
- 报告保存�?`reports` 目录�?

## 界面功能说明

### 配置区域
- **配置文件路径**：指�?YAML 配置文件位置
- **加载配置**：读取并应用配置

### 工作目录区域
- **工作目录**：指定要审核�?SVN 仓库路径
- **扫描变更**：检测该目录下的所有变更文�?

### 文件列表区域
- 显示所有检测到的变更文�?
- 支持多�?单�?
- 显示文件状态和路径

### 执行日志区域
- 实时显示操作日志
- 包含时间�?
- 自动滚动到最新日�?

## CLI 模式

如果需要使用命令行模式，可以带参数运行�?

```bash
# 审核当前目录
svn-ai-reviewer.exe review

# 审核指定目录
svn-ai-reviewer.exe review -d /path/to/repo

# 交互式选择文件
svn-ai-reviewer.exe review -i

# 查看帮助
svn-ai-reviewer.exe review --help
```

## 常见问题

### Q: 浏览器没有自动打开怎么办？
A: 手动在浏览器中访�?`http://localhost:8080`

### Q: 端口 8080 被占用怎么办？
A: 目前端口是固定的，请先关闭占�?8080 端口的程�?

### Q: 如何停止服务器？
A: 在命令行窗口�?`Ctrl+C` 或直接关闭窗�?

### Q: 配置文件在哪里？
A: 默认使用程序目录下的 `config.yaml`，可以参�?`config.example.yaml` 创建

### Q: 审核报告保存在哪里？
A: 默认保存�?`reports` 目录下，文件名包含时间戳

## 技术特�?

- �?�?Go 实现，无需额外依赖
- �?内嵌 Web 界面，单文件部署
- �?跨平台支持（Windows/Linux/Mac�?
- �?现代�?UI 设计
- �?实时日志反馈
- �?保留完整�?CLI 功能

## 注意事项

1. 确保 SVN 命令行工具已安装并可�?
2. 确保配置文件中的 AI API Key 正确
3. 网络连接正常，能够访�?AI 服务
4. 工作目录必须是有效的 SVN 仓库
