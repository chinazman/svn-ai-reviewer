# 快速开�?- SVN在线审核模式

## 5分钟快速上�?

### 方式一：GUI图形界面（推荐新手）

#### 1. 启动程序
```bash
# Windows
svn-ai-reviewer.exe

# Linux/Mac
./svn-ai-reviewer
```

#### 2. 打开在线模式
- 浏览器会自动打开 http://localhost:8080
- 点击顶部�?在线模式"按钮

#### 3. 连接SVN服务�?
```
SVN服务器地址: https://svn.example.com/repo
用户�? your_username
密码: your_password
�?记住凭据
```
点击"连接服务�?

#### 4. 搜索提交记录
```
目录路径: /trunk/src        (可选，留空表示根目�?
关键�? bug fix             (可�?
作�? zhangsan              (可�?
```
点击"搜索"

#### 5. 选择要审核的内容
- 在提交记录列表中勾选要审核的版�?
- 点击"加载选中版本的文�?
- 在文件列表中勾选要审核的文件（默认全选）

#### 6. 开始审�?
- 点击"开始审�?按钮
- 等待审核完成
- 自动打开HTML报告

### 方式二：CLI命令行（推荐高级用户�?

#### 1. 首次使用（保存配置）
```bash
# 远程SVN服务器（需要用户名密码�?
svn-ai-reviewer review online \
  --url https://svn.example.com/repo \
  --username your_username \
  --password your_password \
  --save

# 本地SVN仓库（不需要用户名密码�?
svn-ai-reviewer review online \
  --url file:///F:/02demo/myrepo/trunk \
  --save
```

#### 2. 后续使用（使用保存的凭据�?
```bash
# 搜索特定作者的提交
svn-ai-reviewer review online --author zhangsan

# 搜索包含关键词的提交
svn-ai-reviewer review online --keyword "bug fix"

# 搜索特定路径
svn-ai-reviewer review online --path /trunk/src

# 组合搜索
svn-ai-reviewer review online --path /trunk --author zhangsan --keyword "feature"
```

#### 3. 交互式选择
程序会显示搜索结果，然后提示你：
```
找到 50 条提交记�?
  [1] r1234 | zhangsan | 2024-01-15 10:30:00
      修复登录bug
  [2] r1233 | lisi | 2024-01-15 09:20:00
      添加新功�?
  ...

请输入要审核的版本编号（用逗号分隔，例�? 1,3,5�?
> 1,2

检测到 5 个变更文�?
  [1] [修改] src/login.go (r1234)
  [2] [新增] src/utils.go (r1234)
  ...

请输入要审核的文件编号（用逗号分隔，或输入 'all' 审核所有文件）:
> all

开始审�?5 个文�?..
```

## 常见使用场景

### 场景1：审核同事的最近提�?
```bash
# GUI: 在搜索框输入同事名字
作�? zhangsan

# CLI:
svn-ai-reviewer review online --author zhangsan
```

### 场景2：查找并审核bug修复
```bash
# GUI: 在搜索框输入关键�?
关键�? bug

# CLI:
svn-ai-reviewer review online --keyword "bug"
```

### 场景3：审核特定模块的变更
```bash
# GUI: 在搜索框输入路径
目录路径: /trunk/src/auth

# CLI:
svn-ai-reviewer review online --path /trunk/src/auth
```

### 场景4：审核特定作者在特定模块的提�?
```bash
# GUI: 同时填写多个搜索条件
目录路径: /trunk/src/auth
作�? zhangsan

# CLI:
svn-ai-reviewer review online --path /trunk/src/auth --author zhangsan
```

## 配置文件示例

创建或编�?`config.yaml`�?

```yaml
# AI配置
ai:
  provider: "deepseek"
  api_key: "sk-xxxxxxxxxxxxx"
  base_url: "https://api.deepseek.com/v1"
  model: "deepseek-chat"
  temperature: 0.3
  max_tokens: 2000

# 审核提示�?
review_prompt: |
  你是一个专业的代码审核专家。请审查代码并以JSON格式输出结果�?

# SVN配置
svn:
  command: "svn"

# 在线模式配置（使�?-save后自动填充）
online:
  url: "https://svn.example.com/repo"
  username: "your_username"
  password: "your_password"

# 报告配置
report:
  output_dir: "./reports"
  auto_open: true
```

## 故障排除

### 问题1：连接失�?
```
错误: 连接SVN服务器失�?
```
**解决方案**�?
1. 检查SVN服务器地址是否正确
2. 检查用户名和密码是否正�?
3. 检查网络连�?
4. 确认SVN命令行工具已安装：`svn --version`

### 问题2：搜索无结果
```
没有找到匹配的提交记�?
```
**解决方案**�?
1. 放宽搜索条件（减少关键词�?
2. 检查路径是否正�?
3. 检查作者名是否完全匹配

### 问题3：审核失�?
```
审核失败: API请求失败
```
**解决方案**�?
1. 检查AI API配置是否正确
2. 检查API Key是否有效
3. 检查网络连�?
4. 查看日志获取详细错误信息

### 问题4：密码保存失�?
```
保存凭据失败
```
**解决方案**�?
1. 检查config.yaml文件是否有写权限
2. 确认配置文件路径正确
3. 手动编辑config.yaml添加online配置

## 下一�?

- 📖 阅读完整文档：`在线模式使用说明.md`
- 🔧 自定义审核规则：编辑 `config.yaml` 中的 `review_prompt`
- 📊 查看审核报告：在 `reports` 目录�?
- 💡 了解更多功能：`README.md`

## 技巧和最佳实�?

1. **首次使用记得保存凭据**
   - GUI: 勾�?记住凭据"
   - CLI: 使用 `--save` 参数

2. **使用组合搜索提高精确�?*
   ```bash
   svn-ai-reviewer review online --path /trunk --author zhangsan --keyword "feature"
   ```

3. **审核前先预览文件列表**
   - 可以取消勾选不需要审核的文件
   - 减少审核时间和API调用

4. **定期审核团队代码**
   - 每周审核一次团队成员的提交
   - 及时发现和解决代码质量问�?

5. **保存常用搜索条件**
   - 可以创建shell脚本或批处理文件
   - 快速执行常用的审核任务

## 示例脚本

### Windows批处�?(review-team.bat)
```batch
@echo off
echo 审核团队本周提交...
svn-ai-reviewer.exe review online --author zhangsan --keyword "本周"
pause
```

### Linux/Mac脚本 (review-team.sh)
```bash
#!/bin/bash
echo "审核团队本周提交..."
./svn-ai-reviewer review online --author zhangsan --keyword "本周"
```

使用方法�?
```bash
# Windows
review-team.bat

# Linux/Mac
chmod +x review-team.sh
./review-team.sh
```

---

**祝你使用愉快！如有问题，请查看完整文档或提交Issue�?*
